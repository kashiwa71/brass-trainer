{
  "name": "brass-trainer",
  "private": true,
  "version": "0.1.0",
  "description": "チューバ奏者向けの練習アプリ（音当て・出だし・リップスラー・タンギング）",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "e2e": "node e2e/run.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "playwright-core": "^1.50.0"
  }
}
